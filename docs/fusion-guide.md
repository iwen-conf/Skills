# Fusion Guide

Arc 只保留 `arc:*` 软件工程生命周期 Skill。组合原则很简单：

- 任务编排、记忆和协作交给 `aitask`。
- Arc 提供定义、澄清、可选飞书工作区、交付、前端工程、修复、审计七类工作方法。
- 飞书是可选能力：已有 `.lark.json` 时由 `arc:docs` 检查和更新；没有 `.lark.json` 时，只有用户明确触发或确认后才创建飞书资源。
- 垂直领域能力交给对应 Skill，不在 Arc 内重复建设；Arc 只记录必要链接和生命周期状态。
