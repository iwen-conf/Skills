interface:
  display_name: "Project Architecture Conventions"
  short_description: "Backend architecture, DIP, zap logging, and Go stdlib constants"
  default_prompt: "Use $project-architecture-conventions before writing backend code to apply the default architecture naming, layering, interface design, DIP, zap logging, and Go standard-library constant rules."
